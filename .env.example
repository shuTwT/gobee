STAGE=dev # dev or prod
# Base64 编码的 32 字节 AES-256 密钥，用于加密数据库中的 AI API Key
# 主要配置方式：data/config.toml 的 [ai] config_encryption_key，首次启动缺失时自动生成
# 如仍希望用环境变量覆盖，设置此值（优先级高于配置文件）；生成方式：openssl rand -base64 32
# 注意：密钥更换后，数据库中旧密文将无法解密，请勿随意变更
AI_CONFIG_ENCRYPTION_KEY=YOUR_KEY
